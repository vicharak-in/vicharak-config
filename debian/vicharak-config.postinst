#!/bin/sh

set -e

action="$1"

if [ "$action" = configure ]; then
	if [ ! -e /config/config.txt ]; then
		cp -f /usr/share/doc/vicharak-config/config.txt /usr/lib/vicharak-config/conf.d
	fi
fi

#DEBHELPER#

exit 0
